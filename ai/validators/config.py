"""
검증기 설정 상수들
"""

# 하이퍼 파라미터 상수
REJECT_THRESHOLD = 0.6      # 거부 임계값 (60% 이상)
REVIEW_THRESHOLD = 0.4      # 검토 임계값 (40% 이상)
VIOLATION_THRESHOLD = 0.4   # 위반 수집 임계값

# 검색 및 표시 관련 상수
REJECTION_REASONS_SEARCH_K = 6  # 반려 사유 검색 개수
EVIDENCE_LIMIT = 3          # evidence 수집 개수
VIOLATION_DISPLAY_LIMIT = 3 # 위반 사항 표시 개수
TEXT_PREVIEW_LENGTH = 100   # 텍스트 미리보기 길이

# 블랙리스트 정규식 패턴들
BLACKLIST_PATTERNS = [
    # 1. 포인트/쿠폰 안내
    (r'(포인트|마일리지|적립금|쿠폰).*(적립|지급|소멸|만료)', '포인트/쿠폰 안내'),
    
    # 2. 단기 소멸 쿠폰
    (r'(쿠폰).*(오늘|내일|당일|24\s*시간|단기|짧은\s*기간|즉시).*(소멸|만료)', '단기 소멸 쿠폰'),
    
    # 3. 장바구니/클릭 상품 안내
    (r'(장바구니|찜|최근\s*본|클릭).*(상품|구매|결제|확인|주문)', '장바구니/클릭 상품 안내'),
    
    # 4. 특가 상품 알림
    (r'(특가|초특가|한정\s*특가|단독\s*특가)', '특가 상품 알림'),
    
    # 5. 주식 종목 추천
    (r'(주식|종목|급등주|추천주|투자\s*정보|매수|매도)', '주식 종목 추천'),
    
    # 6. 광고성 URL 혼합
    (r'(주문완료|배송완료|결제완료).*(http[s]?:\/\/\S*특가|\S*이벤트|\S*할인)', '광고성 URL 혼합'),
    
    # 7. 대출 정보 안내
    (r'(대출|신용|한도|이자|금리).*(안내|신청|추천|상품)', '대출 정보 안내'),
    
    # 8. 앱 다운로드 유도
    (r'(앱|어플|application|app).*(다운로드|설치|받으세요|설치해)', '앱 다운로드 유도'),
    
    # 9. 무료 구독형 콘텐츠
    (r'(무료).*(뉴스레터|구독|정기\s*발행|콘텐츠)', '무료 구독형 콘텐츠'),
    
    # 10. 일반 공지
    (r'(연말|연시|명절|감사\s*인사|시즌\s*인사)', '일반 공지'),
    
    # 11. 단순 생일 축하
    (r'(생일\s*축하|생신\s*축하|birthday)', '단순 생일 축하'),
    
    # 12. 수술/진료 후 안부
    (r'(수술|진료|검사).*(안부|쾌유|회복|건강)', '수술/진료 후 안부'),
    
    # 13. 통화 후 자동 발송
    (r'(통화|전화).*(종료|끝난).*(안내|자동\s*발송)', '통화 후 자동 발송'),
    
    # 14. 채널 추가/수신 동의 안내
    (r'(채널\s*추가|수신\s*동의|정보통신망법).*(감사|확인)', '채널 추가/수신 동의 안내'),
    
    # 15. 변수로만 구성
    (r'^(#\{\w+\}\s*)+$', '변수로만 구성'),
    
    # 16. 금융 결제/송금 유도
    (r'(결제|송금|납부).*(해주세요|진행|유도|바랍니다)', '금융 결제/송금 유도'),
    
    # 17. 설문조사 보상
    (r'(설문조사|설문\s*참여).*(포인트|쿠폰|적립금|보상)', '설문조사 보상'),
    
    # 18. 연령 인증 콘텐츠
    (r'(19세|성인\s*인증|연령\s*인증|청소년\s*불가|주류|성인\s*콘텐츠)', '연령 인증 콘텐츠'),
]
