# 카카오 소셜 로그인 기능 가이드

## 개요

이 프로젝트는 카카오 OAuth2를 이용한 소셜 로그인 기능을 제공합니다. 사용자는 기존의 이메일/비밀번호 로그인 외에도 카카오 계정으로 간편하게 로그인할 수 있습니다.

## 아키텍처 및 흐름도

### 1. 전체 카카오 로그인 흐름

```
1. 사용자가 카카오 로그인 버튼 클릭
2. 프론트엔드에서 카카오 인증 서버로 리다이렉트
3. 사용자가 카카오에서 로그인 및 권한 동의
4. 카카오가 인가코드(authorization code)와 함께 콜백 URL로 리다이렉트
5. 프론트엔드가 인가코드를 백엔드로 전송
6. 백엔드가 인가코드로 카카오 API를 호출하여 액세스 토큰 획득
7. 백엔드가 액세스 토큰으로 카카오 사용자 정보 조회
8. 백엔드가 사용자 정보로 회원가입/로그인 처리
9. 백엔드가 JWT 토큰을 생성하여 프론트엔드로 반환
10. 프론트엔드가 JWT 토큰을 저장하고 로그인 완료
```

## 백엔드 구현

### 1. 환경 설정

#### `.env` 파일 설정
```env
# Kakao OAuth2 Configuration
KAKAO_CLIENT_ID=7c3b476fae4e82e8b734efb78e87334d
KAKAO_CLIENT_SECRET=8b3e1152b606a97349902efdf81a1071
KAKAO_REDIRECT_URI=http://127.0.0.1:8080/api/auth/kakao/callback
```

#### `application.yml` 설정
```yaml
spring:
  # OAuth2 설정
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: ${KAKAO_REDIRECT_URI:http://localhost:8080/api/auth/kakao/callback}
            scope:
              - profile_nickname
              - account_email
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
```

### 2. 주요 컴포넌트

#### KakaoController (`/back/src/main/java/com/example/controller/KakaoController.java`)
카카오 소셜 로그인의 엔드포인트를 제공하는 컨트롤러입니다.

**주요 엔드포인트:**
- `POST /api/auth/kakao/login` - 카카오 인가코드로 로그인 처리
- `GET /api/auth/kakao/url` - 카카오 로그인 URL 생성
- `GET /api/auth/kakao/callback` - 카카오 OAuth2 콜백 처리

```java
@PostMapping("/login")
public ResponseEntity<Map<String, String>> kakaoLogin(@RequestParam("code") String authorizationCode) {
    Map<String, String> tokens = kakaoService.processKakaoLogin(authorizationCode);
    return ResponseEntity.ok(tokens);
}
```

#### KakaoService (`/back/src/main/java/com/example/service/KakaoService.java`)
카카오 OAuth2 로그인의 핵심 비즈니스 로직을 처리하는 서비스입니다.

**주요 메서드:**
1. `getAccessToken(String authorizationCode)` - 인가코드로 액세스 토큰 획득
2. `getUserInfo(String accessToken)` - 액세스 토큰으로 사용자 정보 조회
3. `processKakaoLogin(String authorizationCode)` - 전체 카카오 로그인 프로세스 처리
4. `findOrCreateAccount(KakaoUserInfo kakaoUserInfo)` - 사용자 계정 찾기/생성

**카카오 API 호출 과정:**
```java
// 1. 인가코드 → 액세스 토큰
String kakaoAccessToken = webClient.post()
    .uri("https://kauth.kakao.com/oauth/token")
    .contentType(MediaType.APPLICATION_FORM_URLENCODED)
    .bodyValue("grant_type=authorization_code" +
             "&client_id=" + kakaoClientId +
             "&client_secret=" + kakaoClientSecret +
             "&redirect_uri=" + kakaoRedirectUri +
             "&code=" + authorizationCode)
    .retrieve()
    .bodyToMono(Map.class)
    .block();

// 2. 액세스 토큰 → 사용자 정보
KakaoUserInfo userInfo = webClient.get()
    .uri("https://kapi.kakao.com/v2/user/me")
    .header("Authorization", "Bearer " + accessToken)
    .retrieve()
    .bodyToMono(KakaoUserInfo.class)
    .block();
```

#### KakaoUserInfo (`/back/src/main/java/com/example/dto/KakaoUserInfo.java`)
카카오 API로부터 받은 사용자 정보를 담는 DTO 클래스입니다.

**구조:**
```java
public class KakaoUserInfo {
    private Long id;                    // 카카오 사용자 ID
    private String connectedAt;         // 연결 시각
    private Properties properties;      // 프로필 정보
    private KakaoAccount kakaoAccount;  // 카카오계정 정보

    public static class Properties {
        private String nickname;        // 닉네임
    }

    public static class KakaoAccount {
        private String email;          // 이메일
        private Profile profile;       // 프로필
        // 각종 동의 플래그들...
    }
}
```

#### WebClientConfig (`/back/src/main/java/com/example/config/WebClientConfig.java`)
KakaoService에서 사용하는 WebClient Bean을 제공합니다.

```java
@Configuration
public class WebClientConfig {
    @Bean
    public WebClient webClient() {
        return WebClient.builder().build();
    }
}
```

### 3. 사용자 계정 처리 로직

카카오 로그인 시 다음과 같은 로직으로 계정을 처리합니다:

1. **기존 계정 확인**: 카카오에서 받은 이메일로 기존 계정 조회
2. **신규 계정 생성**: 기존 계정이 없으면 새로 생성
   - 이메일: 카카오 계정 이메일 (없으면 `kakao_{카카오ID}@kakao.user`)
   - 사용자명: 카카오 닉네임
   - 비밀번호: `KAKAO_{카카오ID}` (소셜 로그인 식별용)
   - 역할: `USER`
   - 상태: `ACTIVE`

## 프론트엔드 구현

현재 프론트엔드에는 카카오 로그인 전용 컴포넌트가 구현되어 있지 않습니다. `LoginComponent.vue`는 일반 이메일/비밀번호 로그인만 지원합니다.

### 현재 구조
- `LoginComponent.vue`: 기본 로그인 폼 (이메일/비밀번호)
- `authApi`: 기본 인증 API만 정의되어 있음

### 카카오 로그인 프론트엔드 구현 방안

카카오 로그인을 위해서는 다음과 같은 구현이 필요합니다:

#### 1. 카카오 로그인 버튼 추가
```vue
<template>
  <!-- 기존 로그인 폼 -->

  <!-- 구분선 -->
  <v-divider class="my-4">
    <span class="text-caption text-medium-emphasis">또는</span>
  </v-divider>

  <!-- 카카오 로그인 버튼 -->
  <v-btn
    color="#FEE500"
    size="large"
    block
    @click="handleKakaoLogin"
    class="kakao-login-btn"
  >
    <v-icon left>mdi-chat</v-icon>
    카카오 로그인
  </v-btn>
</template>
```

#### 2. 카카오 로그인 처리 로직
```typescript
const handleKakaoLogin = () => {
  // 카카오 인증 URL로 리다이렉트
  const kakaoAuthUrl = `https://kauth.kakao.com/oauth/authorize?client_id=${KAKAO_CLIENT_ID}&redirect_uri=${KAKAO_REDIRECT_URI}&response_type=code&scope=profile_nickname,account_email`
  window.location.href = kakaoAuthUrl
}
```

#### 3. 콜백 처리 페이지
```typescript
// /auth/kakao/callback 라우트에서
const handleKakaoCallback = async () => {
  const urlParams = new URLSearchParams(window.location.search)
  const code = urlParams.get('code')

  if (code) {
    try {
      const response = await api.post('/api/auth/kakao/login', { code })
      // 토큰 저장 및 로그인 처리
      localStorage.setItem('accessToken', response.data.accessToken)
      localStorage.setItem('refreshToken', response.data.refreshToken)
      // 메인 페이지로 리다이렉트
      router.push('/')
    } catch (error) {
      console.error('카카오 로그인 실패:', error)
    }
  }
}
```

## 카카오 개발자 콘솔 설정

### 1. 애플리케이션 등록
1. [카카오 개발자 콘솔](https://developers.kakao.com/) 접속
2. 내 애플리케이션 > 애플리케이션 추가하기
3. 앱 이름, 회사명 입력 후 저장

### 2. 플랫폼 설정
1. 앱 설정 > 플랫폼 > Web 플랫폼 등록
2. 사이트 도메인: `http://localhost:3000`, `http://127.0.0.1:3000` 등록

### 3. 카카오 로그인 설정
1. 제품 설정 > 카카오 로그인 > 활성화 설정: ON
2. Redirect URI: `http://127.0.0.1:8080/api/auth/kakao/callback` 등록
3. 동의항목 설정:
   - 닉네임: 필수 동의
   - 카카오계정(이메일): 선택 동의

### 4. 앱 키 확인
1. 앱 설정 > 요약 정보에서 앱 키 확인
2. REST API 키: `KAKAO_CLIENT_ID`로 사용
3. Admin 키: `KAKAO_CLIENT_SECRET`로 사용

## 테스트 및 검증

### 1. 백엔드 테스트
`KakaoControllerTest.java`에서 다음과 같은 테스트가 구현되어 있습니다:
- 카카오 로그인 엔드포인트 테스트
- 카카오 콜백 처리 테스트
- 에러 상황 처리 테스트

### 2. 수동 테스트 방법
1. 카카오 로그인 URL 생성: `GET /api/auth/kakao/url`
2. 브라우저에서 해당 URL로 접속
3. 카카오 로그인 후 콜백 URL로 리다이렉트 확인
4. 인가코드로 로그인: `POST /api/auth/kakao/login`

## 보안 고려사항

### 1. HTTPS 사용
운영 환경에서는 반드시 HTTPS를 사용해야 합니다.

### 2. 클라이언트 시크릿 보안
`KAKAO_CLIENT_SECRET`은 서버 사이드에서만 사용하고, 프론트엔드에 노출되지 않도록 주의해야 합니다.

### 3. 토큰 관리
- JWT 토큰의 만료시간을 적절히 설정
- Refresh Token을 통한 토큰 갱신 로직 구현
- 토큰을 안전한 저장소에 보관

### 4. 사용자 정보 처리
- 최소한의 필요한 정보만 요청
- 개인정보 처리방침에 카카오 로그인 관련 내용 명시

## 문제 해결

### 1. 흔한 오류들

#### `Could not resolve placeholder` 오류
- 환경변수 설정 확인
- application.yml 구조 확인 (spring: 하위에 security 위치)

#### `WebClient` Bean 없음 오류
- `WebClientConfig` 클래스 존재 확인
- `@Configuration` 어노테이션 확인

#### 콜백 URL 불일치
- 카카오 개발자 콘솔의 Redirect URI 설정 확인
- `.env` 파일의 `KAKAO_REDIRECT_URI` 확인

### 2. 로그 확인
KakaoService에는 상세한 로그가 구현되어 있어 문제 해결에 도움이 됩니다:
- 인가코드 수신 로그
- 토큰 요청/응답 로그
- 사용자 정보 조회 로그
- 계정 생성/조회 로그

## 향후 개선 사항

### 1. 프론트엔드 카카오 로그인 구현
현재는 백엔드만 구현되어 있으므로, 프론트엔드 카카오 로그인 버튼과 콜백 처리 로직이 필요합니다.

### 2. 계정 연동 기능
기존 일반 계정과 카카오 계정을 연동하는 기능을 추가할 수 있습니다.

### 3. 추가 소셜 로그인
네이버, 구글 등 다른 소셜 로그인 서비스 추가를 고려할 수 있습니다.

### 4. 사용자 정보 동기화
카카오에서 변경된 사용자 정보를 주기적으로 동기화하는 기능을 추가할 수 있습니다.

## 관련 파일

### 백엔드
- `KakaoController.java` - 카카오 로그인 엔드포인트
- `KakaoService.java` - 카카오 로그인 비즈니스 로직
- `KakaoUserInfo.java` - 카카오 사용자 정보 DTO
- `WebClientConfig.java` - WebClient 설정
- `application.yml` - OAuth2 설정
- `.env` - 카카오 앱 키 설정

### 프론트엔드 (현재 미구현)
- `LoginComponent.vue` - 기본 로그인 컴포넌트 (카카오 로그인 버튼 추가 필요)
- 카카오 콜백 처리 페이지 (신규 구현 필요)

### 테스트
- `KakaoControllerTest.java` - 카카오 로그인 API 테스트

---

이 문서는 현재 구현된 카카오 소셜 로그인 기능을 기반으로 작성되었습니다. 프론트엔드 구현이 완료되면 해당 내용도 업데이트해야 합니다.