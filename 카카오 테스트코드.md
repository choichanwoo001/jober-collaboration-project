
# 카카오 로그인 테스트 코드 가이드

## 개요

이 문서는 카카오 로그인 컨트롤러 테스트 코드의 구조와 동작 방식을 설명합니다. Spring Boot Test와 Mockito를 활용한 단위 테스트의 패턴을 이해할 수 있습니다.

## 테스트 구조 및 어노테이션

### 1. 주요 테스트 어노테이션

#### `@WebMvcTest(KakaoController.class)`
```java
@WebMvcTest(KakaoController.class)
@DisplayName("카카오 로그인 컨트롤러 테스트")
class KakaoControllerTest {
```

**특징:**
- **웹 레이어만 테스트**: 컨트롤러 계층만 로드하여 빠른 단위 테스트
- **자동 구성**: MockMvc, ObjectMapper 등 웹 테스트 관련 Bean 자동 주입
- **범위 제한**: JPA, Service 등 다른 계층은 로드하지 않음
- **장점**: 빠른 테스트 실행, 격리된 테스트 환경

#### `@MockBean`
```java
@MockBean
private KakaoService kakaoService;
```

**특징:**
- **스프링 컨텍스트에 Mock 등록**: 실제 Bean 대신 Mock 객체 주입
- **외부 의존성 제거**: 실제 카카오 API 호출 없이 테스트
- **원하는 동작 시뮬레이션**: `when().thenReturn()` 패턴으로 응답 제어

#### `@DisplayName`
```java
@DisplayName("카카오 로그인 성공 - 정상적인 인가코드로 토큰 반환")
```

**특징:**
- **테스트 이름 한글화**: 테스트 의도를 명확히 표현
- **가독성 향상**: 테스트 결과에서 한글로 표시

### 2. 테스트 구성 요소

#### MockMvc
```java
@Autowired
private MockMvc mockMvc;
```

**역할:**
- **HTTP 요청/응답 시뮬레이션**: 실제 웹 서버 없이 컨트롤러 테스트
- **요청 생성**: `get()`, `post()` 등으로 HTTP 요청 생성
- **응답 검증**: `andExpect()`로 상태 코드, JSON 응답 검증

#### ObjectMapper
```java
@Autowired
private ObjectMapper objectMapper;
```

**역할:**
- **JSON 직렬화/역직렬화**: Java 객체 ↔ JSON 변환
- **테스트 데이터 생성**: 복잡한 요청 Body 생성 시 활용

## Mock 객체 활용 패턴

### 1. Mock 동작 정의 (Stubbing)

#### 성공 케이스
```java
// Given
String authorizationCode = "valid_authorization_code";

// Mock 객체 동작 정의: processKakaoLogin 호출 시 mockTokenResponse 반환
when(kakaoService.processKakaoLogin(authorizationCode))
        .thenReturn(mockTokenResponse);
```

**설명:**
- `when()`: Mock 객체의 특정 메서드 호출 조건 정의
- `thenReturn()`: 해당 조건 만족 시 반환값 지정
- **장점**: 외부 API 호출 없이 예상 응답 시뮬레이션

#### 예외 케이스
```java
// Mock 객체가 예외를 던지도록 설정
when(kakaoService.processKakaoLogin(invalidCode))
        .thenThrow(new RuntimeException("카카오 로그인 실패: 잘못된 인가코드"));
```

**설명:**
- `thenThrow()`: 예외 상황 시뮬레이션
- **활용**: 에러 처리 로직 테스트

### 2. Mock 호출 검증 (Verification)

#### 호출 확인
```java
// Mock 객체 호출 검증
verify(kakaoService, times(1)).processKakaoLogin(authorizationCode);
```

#### 호출되지 않음 확인
```java
// Mock 객체가 호출되지 않았는지 확인
verify(kakaoService, never()).processKakaoLogin(anyString());
```

**검증 옵션:**
- `times(n)`: 정확히 n번 호출
- `never()`: 호출되지 않음
- `atLeast(n)`: 최소 n번 호출
- `atMost(n)`: 최대 n번 호출

## 테스트 케이스 분석

### 1. 카카오 로그인 URL 조회 테스트

```java
@Test
@DisplayName("카카오 로그인 URL 조회 성공")
void 카카오_로그인_URL_조회_성공() throws Exception {
    mockMvc.perform(get("/api/auth/kakao/url")
                    .contentType(MediaType.APPLICATION_JSON))
            .andDo(print()) // 요청/응답 출력
            .andExpect(status().isOk()) // HTTP 200 상태 확인
            .andExpect(jsonPath("$.url").exists()) // url 필드 존재 확인
            .andExpect(jsonPath("$.url").value(containsString("kauth.kakao.com")));
}
```

**테스트 패턴:**
1. **Given**: 특별한 설정 없음 (정적 URL 생성 로직)
2. **When**: GET 요청 수행
3. **Then**: 응답 상태 및 URL 내용 검증

**검증 요소:**
- HTTP 상태 코드: `status().isOk()`
- JSON 필드 존재: `jsonPath("$.url").exists()`
- JSON 값 검증: `jsonPath("$.url").value(containsString(...))`

### 2. 카카오 로그인 성공 테스트

```java
@Test
@DisplayName("카카오 로그인 성공 - 정상적인 인가코드로 토큰 반환")
void 카카오_로그인_성공() throws Exception {
    // Given
    String authorizationCode = "valid_authorization_code";
    when(kakaoService.processKakaoLogin(authorizationCode))
            .thenReturn(mockTokenResponse);

    // When & Then
    mockMvc.perform(post("/api/auth/kakao/login")
                    .param("code", authorizationCode)
                    .contentType(MediaType.APPLICATION_JSON))
            .andDo(print())
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.accessToken").value("mock_access_token"))
            .andExpect(jsonPath("$.refreshToken").value("mock_refresh_token"))
            .andExpect(jsonPath("$.userId").value("12345"))
            .andExpect(jsonPath("$.role").value("USER"));

    verify(kakaoService, times(1)).processKakaoLogin(authorizationCode);
}
```

**테스트 흐름:**
1. **Mock 설정**: 서비스 호출 시 예상 응답 정의
2. **요청 실행**: POST 요청으로 인가코드 전달
3. **응답 검증**: 토큰 정보 및 사용자 정보 확인
4. **호출 검증**: Mock 객체가 올바르게 호출되었는지 확인

### 3. 예외 상황 테스트

```java
@Test
@DisplayName("카카오 로그인 실패 - 서비스에서 예외 발생")
void 카카오_로그인_실패_서비스_예외() throws Exception {
    // Given
    String invalidCode = "invalid_code";
    when(kakaoService.processKakaoLogin(invalidCode))
            .thenThrow(new RuntimeException("카카오 로그인 실패: 잘못된 인가코드"));

    // When & Then
    mockMvc.perform(post("/api/auth/kakao/login")
                    .param("code", invalidCode)
                    .contentType(MediaType.APPLICATION_JSON))
            .andDo(print())
            .andExpect(status().isBadRequest())
            .andExpect(jsonPath("$.error").exists())
            .andExpect(jsonPath("$.error").value(containsString("카카오 로그인 실패")));

    verify(kakaoService, times(1)).processKakaoLogin(invalidCode);
}
```

**예외 테스트 패턴:**
1. **예외 시뮬레이션**: `thenThrow()`로 예외 상황 재현
2. **에러 응답 검증**: HTTP 400 상태 및 에러 메시지 확인
3. **호출 검증**: 예외가 발생해도 메서드가 호출되었는지 확인

## 테스트 데이터 관리

### @BeforeEach를 활용한 테스트 데이터 준비

```java
private Map<String, String> mockTokenResponse;

@BeforeEach
void setUp() {
    // 목 응답 데이터 준비
    mockTokenResponse = new HashMap<>();
    mockTokenResponse.put("accessToken", "mock_access_token");
    mockTokenResponse.put("refreshToken", "mock_refresh_token");
    mockTokenResponse.put("userId", "12345");
    mockTokenResponse.put("role", "USER");
}
```

**장점:**
- **재사용성**: 여러 테스트에서 동일한 데이터 활용
- **일관성**: 모든 테스트에서 동일한 응답 구조 보장
- **유지보수**: 응답 구조 변경 시 한 곳만 수정

## JsonPath를 활용한 JSON 응답 검증

### 기본 검증 패턴

```java
// 필드 존재 확인
.andExpect(jsonPath("$.url").exists())

// 값 직접 비교
.andExpect(jsonPath("$.accessToken").value("mock_access_token"))

// 문자열 포함 확인
.andExpect(jsonPath("$.url").value(containsString("kauth.kakao.com")))

// 타입 확인
.andExpect(jsonPath("$.userId").isString())
```

### 복잡한 JSON 구조 검증

```java
// 배열 크기 확인
.andExpect(jsonPath("$.items", hasSize(3)))

// 배열 요소 확인
.andExpect(jsonPath("$.items[0].name").value("item1"))

// 중첩 객체 확인
.andExpect(jsonPath("$.user.profile.nickname").value("홍길동"))
```

## 테스트 실행 및 결과 확인

### Gradle을 통한 테스트 실행

```bash
# 모든 테스트 실행
./gradlew test

# 특정 테스트 클래스 실행
./gradlew test --tests KakaoControllerTest

# 특정 테스트 메서드 실행
./gradlew test --tests KakaoControllerTest.카카오_로그인_성공

# 테스트 결과 상세 출력
./gradlew test --info
```

### 테스트 결과 보고서

테스트 실행 후 `build/reports/tests/test/index.html`에서 상세한 테스트 결과를 확인할 수 있습니다.

## Mock vs Real 테스트 비교

### Mock 테스트 (현재 구현)

**장점:**
- **빠른 실행**: 외부 API 호출 없음
- **안정적**: 외부 서비스 상태에 영향받지 않음
- **제어 가능**: 다양한 시나리오 시뮬레이션
- **격리된 테스트**: 단위 테스트 원칙 준수

**단점:**
- **실제 통합 검증 불가**: 실제 카카오 API와의 연동 확인 안됨
- **Mock 설정 복잡성**: 복잡한 시나리오일수록 Mock 설정 어려움

### 통합 테스트 (참고용)

```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@ActiveProfiles("test")
class KakaoControllerIntegrationTest {

    @Autowired
    private TestRestTemplate restTemplate;

    @LocalServerPort
    private int port;

    // 실제 서비스와 통합 테스트...
}
```

**특징:**
- **전체 애플리케이션 로드**: 실제 운영 환경과 유사
- **실제 의존성 사용**: Mock 없이 실제 서비스 호출
- **느린 실행**: 애플리케이션 전체 로드 필요

## 테스트 작성 모범 사례

### 1. AAA 패턴 (Arrange-Act-Assert)

```java
@Test
void 테스트_메서드() {
    // Arrange (Given): 테스트 데이터 준비
    String authorizationCode = "test_code";
    when(kakaoService.processKakaoLogin(authorizationCode))
            .thenReturn(mockTokenResponse);

    // Act (When): 실제 동작 실행
    ResultActions result = mockMvc.perform(post("/api/auth/kakao/login")
            .param("code", authorizationCode));

    // Assert (Then): 결과 검증
    result.andExpect(status().isOk())
          .andExpect(jsonPath("$.accessToken").exists());
}
```

### 2. 테스트 메서드 명명 규칙

```java
// 좋은 예: 상황_동작_결과
void 카카오_로그인_성공()
void 유효하지않은_인가코드로_로그인_실패()
void 카카오_서버_에러_시_적절한_에러_응답()

// 나쁜 예
void test1()
void kakaoLoginTest()
void testLogin()
```

### 3. 하나의 테스트, 하나의 관심사

```java
// 좋은 예: 성공 케이스만 테스트
@Test
void 카카오_로그인_성공() {
    // 성공 시나리오만 검증
}

// 나쁜 예: 여러 케이스를 한 테스트에서 검증
@Test
void 카카오_로그인_테스트() {
    // 성공 케이스도 검증하고
    // 실패 케이스도 검증하고
    // 예외 케이스도 검증... (X)
}
```

## 고급 Mock 활용

### ArgumentCaptor 활용

```java
@Captor
private ArgumentCaptor<String> codeCaptor;

@Test
void 인가코드_전달_확인() {
    // When
    kakaoController.kakaoLogin("test_code");

    // Then
    verify(kakaoService).processKakaoLogin(codeCaptor.capture());
    assertThat(codeCaptor.getValue()).isEqualTo("test_code");
}
```

### ArgumentMatchers 활용

```java
// 임의의 문자열 매칭
verify(kakaoService).processKakaoLogin(anyString());

// 특정 패턴 매칭
verify(kakaoService).processKakaoLogin(argThat(code ->
    code.startsWith("valid_")));

// null 값 매칭
verify(kakaoService).processKakaoLogin(isNull());
```

## 문제 해결

### 1. 흔한 테스트 오류

#### MockBean 주입 실패
```
No qualifying bean of type 'KakaoService' available
```
**해결책**: `@MockBean` 어노테이션 확인 및 올바른 패키지 import

#### JSON 응답 매칭 실패
```
Expected: is "expected_value"
Actual: "actual_value"
```
**해결책**: `andDo(print())`로 실제 응답 확인 후 기대값 수정

#### Mock 호출 검증 실패
```
Wanted but not invoked: kakaoService.processKakaoLogin("test_code")
```
**해결책**: 실제 컨트롤러 로직 실행 경로 확인

### 2. 디버깅 팁

#### 요청/응답 출력
```java
.andDo(print()) // 콘솔에 HTTP 요청/응답 출력
```

#### Mock 호출 이력 확인
```java
verifyNoMoreInteractions(kakaoService); // 예상치 못한 호출 확인
```

#### JSON 응답 구조 확인
```java
.andDo(result -> {
    String content = result.getResponse().getContentAsString();
    System.out.println("Response: " + content);
});
```

## 테스트 커버리지

### JaCoCo를 활용한 코드 커버리지 측정

```bash
# 테스트 실행 및 커버리지 측정
./gradlew test jacocoTestReport

# 커버리지 보고서 확인
open build/reports/jacoco/test/html/index.html
```

### 커버리지 목표
- **라인 커버리지**: 80% 이상
- **브랜치 커버리지**: 70% 이상
- **메서드 커버리지**: 90% 이상

## 결론

이 카카오 로그인 테스트 코드는 다음과 같은 테스트 기법을 보여줍니다:

1. **@WebMvcTest**: 웹 레이어 단위 테스트
2. **@MockBean**: 외부 의존성 Mock 처리
3. **MockMvc**: HTTP 요청/응답 시뮬레이션
4. **JsonPath**: JSON 응답 검증
5. **Mockito**: Mock 객체 동작 정의 및 검증

이러한 패턴을 활용하여 안정적이고 빠른 단위 테스트를 작성할 수 있으며, 실제 카카오 API 연동 없이도 모든 시나리오를 검증할 수 있습니다.

테스트 코드는 코드의 품질을 보장하고, 리팩토링 시 안전망 역할을 하므로 지속적으로 관리하고 개선해야 합니다.